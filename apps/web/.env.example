# Base URL of the tRPC API. The local dev server (apps/api) serves tRPC under the
# `/trpc` path and allows CORS from http://localhost:5173. On Render the image bakes
# a same-origin `/trpc` (see render.yaml). Override for a deployed API Gateway endpoint.
VITE_API_URL=http://localhost:3001/trpc
