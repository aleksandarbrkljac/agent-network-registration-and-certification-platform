node_modules/
dist/
.turbo/
coverage/
*.tsbuildinfo

# Prisma / SQLite (demo persistence — lives in packages/db)
*.db
*.db-journal
packages/db/prisma/dev.db*

# Demo videos live in the PR description, not the repo
docs/*.mp4

# Env
.env
.env.local
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Local agent tooling (Claude Code working notes — not part of the deliverable)
.claude/
